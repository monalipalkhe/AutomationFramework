pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Add build steps here, e.g., compile, package
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Add test steps here, e.g., run unit tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deployment steps here
            }
        }
    }
}